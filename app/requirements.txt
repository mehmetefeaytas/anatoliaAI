# Anatolia AI — yarışma çözümü bağımlılıkları
# KISIT: yalnızca açık kaynak (Apache/MIT/BSD); ücretli API/servis YOK.
# Deterministik çekirdek (normalization + rules + eval) saf stdlib ile çalışır;
# aşağıdakiler scraping / LLM / API / DB katmanları içindir.

# --- çekirdek / şema ---
pydantic>=2.6            # LLM guided_json şemaları (MIT)

# --- scraping ---
requests>=2.31           # Apache-2.0 (scrape_mode: static)
beautifulsoup4>=4.12     # MIT (bağlantı çıkarımı + metin ayıklama)
playwright>=1.42         # Apache-2.0 (scrape_mode: js — Nuxt/SPA bankalar)
pyyaml>=6.0              # MIT (config/banks.yaml; yoksa config._mini_parse'a düşer)
pypdf>=4.0               # BSD-3-Clause — PDF ücret tarifeleri + ürün bilgi formları
# NEDEN: ücret/komisyon tarifeleri ve "Ürün Bilgi Formu" belgeleri HTML tablo
# DEĞİL PDF olarak yayımlanıyor (2026-08-03 tarayıcı doğrulaması: Emlak, Dünya,
# Türkiye Finans). Kesin tahsis ücreti/masraf oranı orada → §18-2 çelişki tespiti.
# PyMuPDF (fitz) KULLANILMAZ: AGPL, Apache-2.0 teslimle uyumsuz (§5.10 lisans riski).
# Kurulu değilse src/scraping/pdf.py boş metin + açık gerekçe döner, hat çökmez.
# NOT: playwright ayrıca tarayıcı ikilisi ister: `playwright install chromium`.
# Kurulu değilse fetcher.BrowserFetcher zarifçe atlar, sistem çökmez.
# robots.txt uyumu (CLAUDE.md §14) src/scraping/robots.py'de, saf stdlib ile.

# OPSİYONEL — lisansı doğrulanana kadar teslim imajına ALINMAZ.
# Bu satır önceden "GPLv3+" diye işaretliydi; GPLv3, Apache-2.0 ile birlikte
# dağıtımda uyumsuzdur (şartname §5.10 "lisans problemi çıkarma potansiyeli"
# + §8 "Apache-2.0 ile paylaşılacak"). Kod bu paket olmadan çalışır:
# collector._extract_main_text, paket yoksa preprocessing.clean.strip_html'e
# (saf stdlib) düşer. Doğrulama adımları: docs/model-license-audit.md §2
# trafilatura>=1.8

# --- TR morfoloji ---
# KALDIRILDI (31 Tem 2026). Kod tabanında sıfır referansı vardı ve ihtiyaç da
# yok: Türkçe gereksinimlerini `preprocessing.clean.tr_fold` (TR-doğru
# katlama — `str.lower()` Türkçede hatalıdır) ve
# `extraction/rules/synonyms.py` (eşanlamlı sözlüğü) saf stdlib ile
# karşılıyor. Şartname §9 "bağımlılıkların eksiksiz listesi" istiyor;
# kullanılmayanı ilan etmek listeyi yanıltıcı yapar.
# zeyrek>=0.1.3          # MIT (saf Python; Zemberek alternatifi)

# --- NLP / model (tamamlayıcı) ---
# KULLANILMIYOR — 3 katmanlı mimarinin "tamamlayıcı NER" katmanı henüz yok.
# gliner>=0.2            # Apache-2.0 (tamamlayıcı NER)
transformers>=4.40       # Apache-2.0 (BERTurk sınıflandırıcı)
# KULLANILIYOR (31 Tem 2026'dan beri): src/rag/embedding.BgeM3Embedder.
# bge-m3 ağırlıkları MIT (docs/model-license-audit.md §4 ✅).
# Ağırlık yoksa kod AÇIK hata verir ve sistem KeywordRetriever ile çalışır —
# sessizce boş sonuç dönmez.
sentence-transformers>=2.7  # Apache-2.0 (bge-m3 embeddings)

# --- API / DB ---
# NOT: teslim edilen API konteyneri bu dosyayı DEĞİL, requirements-api.txt'i
# kurar (ince imaj). Bu dosya geliştirme ortamının tam listesidir.
fastapi>=0.110           # MIT
uvicorn>=0.29            # BSD
# KULLANILIYOR (31 Tem 2026'dan beri): src/db/postgres.PostgresRepository +
# src/rag/store.PgVectorStore. DATABASE_URL boşsa hiç import edilmez.
psycopg[binary]>=3.1     # LGPL (dinamik bağlı istemci)
# `pgvector` PYTHON PAKETİ KALDIRILDI (31 Tem 2026). pgvector'ün kendisi
# sunucu tarafı bir PostgreSQL EKLENTİSİDİR ve kullanılmaya devam ediyor
# (pgvector/pgvector:pg16 imajı + `CREATE EXTENSION vector` + `<=>` operatörü).
# Python istemci paketi yalnızca tip adaptasyonu sağlıyordu; `src/rag/store.py`
# vektörü pgvector'ün belgelenmiş metin biçimiyle ('[0.1,...]') gönderip
# sunucuda `CAST(... AS vector)` yapıyor. Aynı iş, bir bağımlılık eksiğiyle.
# pgvector>=0.2          # PostgreSQL lisansı

# --- geliştirme ---
ruff>=0.4                # MIT
black>=24.0              # MIT
pytest>=8.0              # MIT
